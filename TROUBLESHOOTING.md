# 싸준 트러블슈팅

[← README로 돌아가기](./README.md)

## 1. 3,000개 이상 문제 목록의 응답이 느림

### 문제

초기 구현에서는 `/api/problems` 요청마다 문제 목록과 함께 제출 통계, 시도자 수, 해결자 수, 정답률, 사용자별 풀이 상태를 모두 계산했습니다. 문제 수가 늘자 목록 응답이 약 7~8초까지 느려졌습니다.

### 해결

공용 문제 데이터와 사용자별 풀이 상태를 분리했습니다. 문제별 통계는 `problem_stats`에 사전 집계하고, 정렬·필터·페이지네이션은 PostgreSQL RPC `get_visible_problems_sorted`에서 처리했습니다. 공개 문제 목록 응답에는 `s-maxage=300`과 `stale-while-revalidate=60` 캐시 정책을 적용했습니다.

### 결과

`/api/problems` 응답 시간을 약 7~8초에서 **120ms 수준**으로 단축했습니다. 목록을 다시 볼 때에는 공용 데이터 캐시를 활용하고, 사용자별 상태만 필요한 범위에서 갱신합니다.

---

## 2. 문제 상세 진입 시 에디터와 사용자 데이터가 핵심 콘텐츠를 막음

### 문제

문제 본문, 사용자 풀이 상태, 테스트케이스, 코드 에디터를 한 번에 초기화하면서 사용자가 가장 먼저 필요한 문제 본문 표시가 늦어졌습니다.

### 해결

문제 본문은 Server Component에서 먼저 렌더링하고, 사용자별 상태는 `/api/problems/[id]/solve-state`로 분리했습니다. 무거운 코드 에디터는 동적 로딩과 skeleton UI로 초기 렌더링 경로에서 분리했습니다.

### 결과

초기 요청 수를 **84개 → 27개**, 전송량을 약 **836KB → 210KB**로 줄였습니다. FCP는 약 **1.5초 → 0.3초**, LCP는 약 **2.1초 → 0.3초**로 개선했습니다.

---

## 3. 난이도·언어 표기가 데이터마다 달라 필터가 흔들림

### 문제

난이도 값이 `MEDIUM_HARD`, `Medium-Hard`, `medium hard`처럼 여러 형태로 저장되거나 전달돼, 동일한 필터가 환경에 따라 다른 결과를 낼 수 있었습니다. 언어명도 화면마다 표기가 달라질 여지가 있었습니다.

### 해결

API 경계에서 난이도 값을 정규화해 허용 값 목록으로 변환하고, 정렬 가능한 필드를 화이트리스트로 제한했습니다. 언어 표기는 공통 변환 유틸리티를 통해 표시합니다.

### 결과

URL 쿼리·DB 데이터·UI 표현 간의 차이를 한 곳에서 흡수해 필터와 표의 동작을 일관되게 유지했습니다.
