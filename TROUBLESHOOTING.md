# 싸준 트러블슈팅

[← README로 돌아가기](./README.md)

## 1. 3,000개 이상 문제 목록의 응답이 느림

### 문제

초기 구현에서는 문제 목록 RPC가 요청마다 전체 제출 통계, 시도자 수, 해결자 수, 정답률, 사용자별 풀이 상태를 함께 집계했습니다. 문제 수가 3,000개 이상으로 늘자 브라우저 기준 API 응답 시간이 약 7.9초까지 증가했습니다.

### 해결

공용 문제 데이터와 사용자별 풀이 상태를 분리했습니다. 문제별 통계는 `problem_stats`에 사전 집계하고 Trigger로 갱신했으며, `problems` 테이블과 JOIN하도록 RPC를 재설계했습니다. 정렬·필터·페이지네이션은 PostgreSQL RPC `get_visible_problems_sorted`에서 처리하고, 공개 문제 목록에는 `s-maxage=300`과 `stale-while-revalidate=60` 캐시 정책을 적용했습니다.

### 결과

브라우저 기준 `/api/problems` 응답 시간을 **7.9초 → 0.22초**로 단축했습니다. 목록을 다시 볼 때에는 공용 데이터 캐시를 활용하고, 사용자별 상태만 필요한 범위에서 갱신합니다.

---

## 2. 문제 상세 진입 시 에디터와 사용자 데이터가 핵심 콘텐츠를 막음

### 문제

문제 본문, 사용자 풀이 상태, 테스트케이스, 코드 에디터를 한 번에 초기화하면서 사용자가 가장 먼저 필요한 문제 본문 표시가 늦어졌습니다.

### 해결

문제 본문은 Server Component에서 먼저 렌더링하고, 사용자별 상태는 `/api/problems/[id]/solve-state`로 분리했습니다. 무거운 코드 에디터는 동적 로딩과 skeleton UI로 초기 렌더링 경로에서 분리했습니다.

### 결과

초기 요청 수를 **84개 → 27개**, 전송량을 약 **836KB → 210KB**로 줄였습니다. FCP는 약 **1.5초 → 0.3초**, LCP는 약 **2.1초 → 0.3초**, Lighthouse 점수는 **89점 → 100점**으로 개선했습니다.

---

## 3. 홈 진입에 필요하지 않은 리소스가 초기 렌더링을 막음

### 문제

홈 화면 진입 시 KaTeX, 웹폰트, Markdown 렌더링과 반복 링크 prefetch가 함께 실행됐고, 사용자별 제출 데이터까지 초기 경로에서 조회해 핵심 콘텐츠가 늦게 도착했습니다.

### 해결

홈에서 사용하지 않는 전역 리소스를 제거하고 주요 링크의 prefetch를 제한했습니다. 사용자별 제출 데이터는 클라이언트에서 지연 로드하도록 분리했습니다.

### 결과

초기 렌더링 경로에서 불필요한 네트워크·번들 비용을 제거해, Lighthouse **89점 → 100점**과 요청 수 **84개 → 27개** 개선으로 이어졌습니다.
